# Shows sys.path details via prefix values.
# ------------------------------------------------------------------------------
# Prints prefix values from sys.path.
import sys
print(sys.prefix)
print(sys.exec_prefix)
